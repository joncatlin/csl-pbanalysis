Working pattern for examples below
Pattern=^\s*OPEN\s+(?<filename>.*)\s+TO\s+(?<to>\S+)


      OPEN 'DEP' TO DEP.FILE ELSE EMSG<-1>=CO:'DEP'
      OPEN 'DICT','DEP' TO D.DEP ELSE EMSG<-1>=CO:'DICT, DEP'
      OPEN 'HDEP' TO HD.FILE ELSE EMSG<-1>=CO:"HDEP"
      OPEN 'MONTHS':SEC TO MONTHS ELSE ABORT 201,'MONTHS':SEC
            OPEN 'DICT ':FILE.NAME TO DICT.FILE ELSE
      OPEN 'CON.TRANS.':MS.CON.HIST.FLAG TO CON.TRANS.MS ELSE FILE.ERR<-1>='CON.TRANS.':MS.CON.HIST.FLAG
      OPEN FILE.NAME TO MAIN.FILE ELSE
               OPEN "1098,":TAXYR TO FILE1098 THEN
            OPEN "1098.XREF,":LASTYR TO XREF1098 THEN
               OPEN 'QF.CONTRACTS.':YEACCT TO EOYCONTRACTS THEN
      OPEN 'UDEP' TO UD.FILE ELSE EMSG<-1>=CO:"UDEP"
      OPEN 'CTRL.FILE' TO CTRL.FILE ELSE EMSG<-1>=CO:'CTRL.FILE'
      OPEN 'CONTRACTS' TO CONTRACTS ELSE EMSG<-1>=CO:'CONTRACTS'
      OPEN 'MAINTENANCE' TO MAINTENANCE ELSE EMSG<-1>=CO:'MAINTENANCE'
      OPEN 'CON.TRANS' TO CT.FILE ELSE EMSG<-1>=CO:'CON.TRANS'
      OPEN 'MO.CON.TRANS' TO MCT.FILE ELSE EMSG<-1>=CO:'MO.CON.TRANS'
      OPEN 'MO.CON.TRANS,NON.CASH' TO MCT.NC ELSE EMSG<-1>=CO:'MO.CON.TRANS,NON.CASH'
      OPEN 'DICT CON.TRANS' TO D.CT.FILE ELSE EMSG<-1>=CO:'DICT CON.TRANS'
      OPEN 'M.TRANS' TO MT.FILE ELSE EMSG<-1>=CO:'M.TRANS'
      OPEN 'MO.M.TRANS' TO MMT.FILE ELSE EMSG<-1>=CO:'MO.M.TRANS'
      OPEN 'MO.M.TRANS,NON.CASH' TO MMT.NC ELSE EMSG<-1>=CO:'MO.M.TRANS,NON.CASH'
      OPEN 'DICT M.TRANS' TO D.MT.FILE ELSE EMSG<-1>=CO:'DICT M.TRANS'
      OPEN "DEP,BATCHES" TO BATCH.FILE ELSE EMSG<-1>=CO:'DEP,BATCH'
      OPEN 'CURR.BAL' TO CURR.BAL ELSE EMSG<-1>=CO:'CURR.BAL'
      OPEN 'CURR.BAL,MAINT' TO CURR.BAL.MAINT ELSE EMSG<-1>=CO:'CURR.BAL,MAINT'
      OPEN 'BANK.BAL' TO BANK.BAL ELSE EMSG<-1>=CO:'BANK.BAL'
      OPEN 'BANK.BAL.AUTO' TO BANK.BAL.AUTO ELSE EMSG<-1>=CO:'BANK.BAL.AUTO'
      OPEN 'LEND.CODES' TO LEND.CODES ELSE EMSG<-1>=CO:'LEND.CODES'
      OPEN 'DEV.CODES' TO DEV.CODES ELSE EMSG<-1>=CO:'DEV.CODES'
      OPEN 'PROJ.CODES' TO PROJ.CODES ELSE EMSG<-1>=CO:'PROJ.CODES'
      OPEN 'ADDR.MAST' TO ADDR.MAST ELSE EMSG<-1>=CO:'ADDR.MAST'
      OPEN 'COLLECT.LIST,IN' TO COLL.IN ELSE EMSG<-1>=CO:'COLLECT.LIST,IN'
      OPEN 'COLLECT.LIST' TO COLLECT.LIST ELSE EMSG<-1>=CO:'COLLECT.LIST'
      OPEN 'COLLECTORS' TO COLLECTORS ELSE EMSG<-1>=CO:'COLLECTORS'
      OPEN 'DICT','COLLECT.LIST' TO DICT.COLL ELSE EMSG<-1>=CO:'DICT COLLECT.LIST'
      OPEN 'R.HIST' TO R.HIST ELSE EMSG<-1>=CO:'R.HIST'
      OPEN 'R.HIST.LFC' TO R.HIST.LFC ELSE EMSG<-1>=CO:'R.HIST.LFC'
      OPEN 'R.HIST.SUN' TO R.HIST.SUN ELSE EMSG<-1>=CO:'R.HIST.SUN'
      OPEN 'OTHER.DATA' TO LITCHFIELD ELSE EMSG<-1>=CO:'OTHER.DATA'
      OPEN 'POOLS' TO POOLS ELSE EMSG<-1>=CO:'POOLS'
      OPEN 'LOW.BAL' TO LOW.BAL ELSE EMSG<-1>=CO:'LOW.BAL'
      OPEN 'LOW.BAL,MAINT' TO LOW.BAL.MAINT ELSE EMSG<-1>=CO:'LOW.BAL,MAINT'
      OPEN 'BANKRUPT' TO BANKRUPT ELSE EMSG<-1>=CO:'BANKRUPT'
      OPEN '&SAVEDLISTS&' TO SAVEDLISTS ELSE EMSG<-1>=CO:'&SAVEDLISTS&'
      OPEN 'STATIC' TO STATIC ELSE EMSG<-1>=CO:'STATIC'
      OPEN 'WEBAIS.XMIT' TO WEBXMIT ELSE EMSG<-1>=CO:'WEBAIS.XMIT'
      OPEN 'COLL.DATA' TO COLL.DATA ELSE EMSG<-1>=CO:'COLL.DATA'
      OPEN 'DELINQ.ME' TO DELINQ.ME ELSE EMSG<-1>=CO:'DELINQ.ME'
      OPEN 'SUREPAY' TO SUREPAY ELSE EMSG<-1>=CO:'SUREPAY'
      OPEN 'RAS.CUST.QUEUE' TO RAS.CUST.QUEUE ELSE EMSG<-1>=CO:'RAS.CUST.QUEUE'
      OPEN 'BANK.ACCT' TO BANK.ACCT.FILE ELSE EMSG<-1>=CO:'BANK.ACCT'
      OPEN 'CONTRACTS,PREVIEW' TO C.PREVIEW ELSE EMSG<-1>=CO:'CONTRACTS,PREVIEW'
      OPEN 'MAINTENANCE,PREVIEW' TO M.PREVIEW ELSE EMSG<-1>=CO:'MAINTENANCE,PREVIEW'
      OPEN 'CHARGES' TO CHARGES.FL ELSE EMSG<-1> = CO:'CHARGES'
      OPEN 'DICT','CHARGES' TO DICT.CHARGES ELSE EMSG<-1> = CO:'DICT CHARGES'
      OPEN 'CHARGES.INDEX' TO CHARGES.INDEX ELSE EMSG<-1> = CO:'CHARGES.INDEX'
      OPEN 'CHARGES.HIST' TO CHARGES.HIST.FL ELSE EMSG<-1> = CO:'CHARGES.HIST'
      OPEN 'CHARGES.HIST.INDEX' TO CHARGES.HIST.INDEX ELSE EMSG<-1> = CO:'CHARGES.HIST.INDEX'
      OPEN 'CHARGES,PREVIEW' TO CHARGES.PREVIEW ELSE EMSG<-1> = CO:'CHARGES,PREVIEW'
      OPEN 'CHARGE.TYPES' TO CHARGE.TYPES.FL ELSE EMSG<-1> = CO:'CHARGE.TYPES'
      OPEN 'BANK.RECON' TO BANK.RECON ELSE EMSG<-1> = CO:'BANK.RECON'
      OPEN 'MERCHACCT' TO MERCHACCT ELSE EMSG<-1> = CO:'MERCHACCT'
      OPEN 'ID.XREF' TO ID.XREF ELSE EMSG<-1> = CO:'ID.XREF'
      OPEN 'JODI.TEMP' TO TEMP.FL ELSE EMSG<-1> = CO:'JODI.TEMP'
      OPEN 'DRAWS,HIST' TO DRAW.FILE ELSE EMSG<-1>=CO:"DRAWS,HIST"
      OPEN 'LATE.CHARGE' TO LATE_CHARGE ELSE EMSG<-1>=CO:'LATE.CHARGE'
      OPEN 'COUPON' TO COUPON ELSE EMSG<-1>=CO:'COUPON'
      OPEN 'TRANS_TYPES' TO TRANS_TYPES ELSE EMSG<-1>=CO:'TRANS_TYPES'
      OPEN 'SBEACH.INT.RATE' TO SBEACH.INT.RATE ELSE EMSG<-1> = CO:'SBEACH.INT.RATE'
      OPEN 'CUSTOM.DATA' TO CUSTOM.DATA ELSE EMSG<-1> = CO:'CUSTOM.DATA'
      OPEN 'STATES' TO STATES ELSE EMSG<-1> = CO:'STATES'
      OPEN 'DFLT.PLACEMENTS' TO DFLT.PLACEMENTS ELSE EMSG<-1> = CO:'DFLT.PLACEMENTS'
      OPEN 'DFLT.AGREEMENT' TO DFLT.AGREEMENT ELSE EMSG<-1> = CO:'DFLT.AGREEMENT'
      OPEN 'LBX.EMAIL' TO LBX.EMAIL ELSE EMSG<-1> = CO:'LBX.EMAIL'
      OPEN 'NOTES.MAST' TO NOTES.MAST ELSE EMSG<-1> = CO:'NOTES.MAST'
      OPEN 'EFT.NSF.CTRL' TO EFT.NSF.CTRL ELSE EMSG<-1> = CO:'EFT.NSF.CTRL'
      OPEN 'CCF.XFER.HIST' TO CCF.XFER.HIST ELSE EMSG<-1> = CO:'CCF.XFER.HIST'
      OPEN 'CFX' TO CFX ELSE EMSG<-1> = CO:'CFX'
      OPEN 'CFX,PROCESSED' TO CFX.PROCESSED ELSE EMSG<-1> = CO:'CFX,PROCESSED'
      OPEN 'CHARGEBACKS' TO CHARGEBACKS ELSE EMSG<-1> = CO:'CHARGEBACKS'
      OPEN 'WEBUSER' TO WEBUSER ELSE EMSG<-1> = CO:'WEBUSER'
      OPEN 'CCF' TO CCF ELSE EMSG<-1> = CO:'CCF'
      OPEN 'COMPANY.TRANSID.CON.TRANS.INDEX' TO COMPANY.TRANSID.CON.TRANS.INDEX ELSE EMSG<-1> = CO:'COMPANY.TRANSID.CON.TRANS.INDEX'
      OPEN 'COMPANY.TRANSID.M.TRANS.INDEX' TO COMPANY.TRANSID.M.TRANS.INDEX ELSE EMSG<-1> = CO:'COMPANY.TRANSID.M.TRANS.INDEX'
      CALL BUILD.TRANS.TYPES.ARRAY(TYPES)
      OPEN '&SAVEDLISTS&' TO SLIST ELSE STOP 201, "&SAVEDLISTS&"
      OPEN 'CONT.CONV' TO CONTRACTS ELSE STOP 201, "CONT.CONV"
      OPEN 'DICT','CONTRACTS' TO DCONTRACTS ELSE STOP 201, "DICT CONTRACTS"
      OPEN 'ID.XREF,CONV' TO ID.XREF.CONV ELSE STOP 201, "ID.XREF,CONV"
      OPEN 'CONTRACTS' TO LIVE.CONTRACTS ELSE STOP 201, "CONTRACTS"
      OPEN 'CONTRACTS,CRUP' TO CRUP.CONTRACTS ELSE STOP 201, "CONTRACTS,CRUP"
      OPEN 'CON.HIST,CONV' TO CON.HIST ELSE STOP 201, "CON.HIST,CONV"
      OPEN 'AM.CONV' TO ADDR.MAST ELSE STOP 201, "AM.CONV"
      OPEN 'ADDR.MAST' TO LIVE.ADDR.MAST ELSE STOP 201, "ADDR.MAST"
      OPEN 'DICT','ADDR.MAST' TO DADDR.MAST ELSE STOP 201, "DICT ADDR.MAST"
      OPEN 'LITCHFIELD,CONV' TO LITCHFIELD ELSE STOP 201, "LITCHFIELD,CONV"5
      OPEN 'DICT','LITCHFIELD' TO DLITCHFIELD ELSE STOP 201, "DICT LITCHFIELD"
      OPEN 'ARMS,CONV' TO ARMS ELSE STOP 201, "ARMS,CONV"
      OPEN 'DICT','ARMS' TO DARMS ELSE STOP 201, "DICT ARMS"
      OPEN 'CTRL.FILE' TO CTRL.FILE ELSE STOP 201, "CTRL.FILE"
      OPEN 'FTP' TO FTP ELSE STOP 201, "FTP"
      OPEN 'CONV.TABLE' TO CONV.TABLE ELSE STOP 201, "CONV.TABLE"
      OPEN 'INVENTORY' TO INVENTORY ELSE STOP 201, "INVENTORY"
!      OPEN 'CAPITAL.ASSET,CONV' TO CAR.FILE ELSE STOP 201, "CAPITAL.ASSET,CONV"
      OPEN 'CONSOLIDATED,CONV' TO CON.FILE ELSE STOP 201, "CONSOLIDATED,CONV"
!      OPEN 'DICT','CAPITAL.ASSET' TO DCAR.FILE ELSE STOP 201, "DICT CAPITAL.ASSET"
      OPEN 'DICT','CONSOLIDATED' TO DCON.FILE ELSE STOP 201, "DICT CONSOLIDATED"
      OPEN 'DICT','MAINT' TO DMAINT ELSE STOP 201, "DICT MAINT"
      OPEN 'MAINT.CONV' TO MAINT ELSE STOP 201, "MAINT.CONV"
      OPEN 'MAINT' TO LIVE.MAINT ELSE STOP 201, "MAINT"
      OPEN 'UNIT.TYPES' TO UNIT.TYPES ELSE STOP 201, "UNIT.TYPES"
      OPEN 'CCF,CONV' TO CCF.CONV ELSE STOP 201, "CCF,CONV"
      OPEN 'DICT','CCF' TO DCC.CONV ELSE STOP 201, "DICT CCF"
      OPEN 'DEV.CODES' TO DEV.CODES ELSE STOP 201, "DEV.CODES"
      OPEN 'PROJ.CODES' TO PROJ.CODES ELSE STOP 201, "PROJ.CODES"
      OPEN 'BANK' TO BANK ELSE STOP 201, "BANK"
      OPEN 'CONV.NOTE' TO CONV.NOTE ELSE STOP 201, "CONV.NOTE"
      OPEN 'GE.CAPITAL,CONV' TO GE.CAPITAL.CONV ELSE STOP 201, "GE.CAPITAL,CONV"
      OPEN 'SKIP.TRACE,CONV' TO SKIP.TRACE.CONV ELSE STOP 201, "SKIP.TRACE,CONV"
      OPEN 'ASSET,CONV' TO ASSET.CONV ELSE STOP 201, "ASSET,CONV"
      OPEN 'DICT','GE.CAPITAL' TO DGECAP.FILE ELSE STOP 201, "DICT GE.CAPITAL"
      OPEN 'DICT','SKIP.TRACE' TO DSKIP.TRACE ELSE STOP 201, "DICT SKIP.TRACE"
      OPEN 'DEALER.XREF' TO DEALER.XREF ELSE STOP 201, "DEALER.XREF"
      OPEN 'COUNTRY' TO COUNTRY.FL ELSE STOP 201, "COUNTRY"
      OPEN 'COUNTRY.INDEX' TO COUNTRY.INDEX ELSE STOP 201,'COUNTRY'
      OPEN 'ID.XREF' TO LIVE.ID.XREF ELSE STOP 201,'ID.XREF'
      OPEN 'ID.XREF2' TO LIVE.ID.XREF2 ELSE STOP 201,'ID.XREF2'
      OPEN 'CONV.TMP' TO CONV.TMP ELSE STOP 201,'CONV.TMP'
      OPEN 'SUREPAY,CONV' TO SUREPAY.CONV ELSE STOP 201,'SUREPAY,CONV'
      OPEN 'STATES' TO STATES ELSE STOP 201,'STATES'
      OPEN 'OBLIG.CUSTOM.DATA' TO OBLIG.CUSTOM.DATA ELSE STOP 201,'OBLIG.CUSTOM.DATA'
      OPEN 'OBLIG.CUSTOM.DATA,CONV' TO CONV.CUSTOM.DATA ELSE STOP 201,'OBLIG.CUSTOM.DATA,CONV'
      OPEN 'CUSTOM.DATA.MAST' TO CD.MAST ELSE STOP 201,'CUSTOM.DATA.MAST'
      OPEN 'CHARGES,CONV' TO CHARGES.CONV ELSE STOP 201,'CHARGES,CONV'
      OPEN 'LEND.CODES' TO LEND.CODES ELSE STOP 201,'LEND.CODES'
      OPEN 'TERMS.ACCEPTED' TO TERMS.ACCEPTED ELSE STOP 201,'TERMS.ACCEPTED'
      OPEN UNITS TO UNITS ELSE STOP 201, "UNITS"
      OPEN 'LOCATIONS' TO LOCATIONS ELSE STOP 201,'LOCATIONS'
      OPEN 'UNIT.INV' TO UNIT.INV ELSE STOP 201,'UNIT.INV'
      OPEN 'INTERVAL.DEF' TO INTERVAL.DEF ELSE STOP 201,'INTERVAL.DEF'
      OPEN 'BANKRUPT,CONV' TO BANKRUPT.CONV ELSE STOP 201,'BANKRUPT,CONV'
      OPEN 'DICT','BANKRUPT' TO DBANKRUPT ELSE STOP 201,'DICT BANKRUPT'
      OPEN 'PROPERTY.INSURANCE,CONV' TO PROPERTY.INSURANCE.CONV ELSE STOP 201,'PROPERTY.INSURANCE,CONV'
      OPEN 'DICT','PROPERTY.INSURANCE' TO DPROPERTY.INSURANCE ELSE STOP 201,'DICT PROPERTY.INSURANCE'
      OPEN 'PROPERTY.TAX,CONV' TO PROPERTY.TAX.CONV ELSE STOP 201,'PROPERTY.TAX,CONV'
      OPEN 'DICT','PROPERTY.TAX' TO DPROPERTY.TAX ELSE STOP 201,'DICT PROPERTY.TAX'
      OPEN 'CONV.ERROR' TO CONV.ERROR ELSE STOP 201,'CONV.ERROR'
      OPEN 'DICT','GK.TAXLOG' TO DGK.TAXLOG ELSE STOP 201,'DICT GK.TAXLOG'
      OPEN 'CONT.COMM2,CONV' TO CONT.COMM2.CONV ELSE STOP 201,'CONT.COMM2,CONV'
      OPEN 'DICT','CONT.COMM2' TO DCONT.COMM2 ELSE STOP 201,'DICT CONT.COMM2'
      OPEN 'SALESMAN' TO SALESMAN ELSE STOP 201,'SALESMAN'
      OPEN 'CHARGES.INDEX' TO CHARGES.INDEX ELSE STOP 201,'CHARGES.INDEX'
      OPEN 'CHARGES' TO LIVE.CHARGES ELSE STOP 201,'CHARGES'
      OPEN 'STATES,INTERNATIONAL' TO STATES.INTERNATIONAL ELSE STOP 201,'STATES,INTERNATIONAL'
      OPEN 'STATES.MEXICO.XREF' TO STATES.MEXICO.XREF ELSE STOP 201,'STATES.MEXICO.XREF'
      OPEN 'MAINT.TAX.RATES' TO MAINT.TAX.RATES ELSE STOP 201,'MAINT.TAX.RATES'
      OPEN 'WJW.SCRATCH' TO WJW.SCRATCH ELSE STOP 201,'WJW.SCRATCH'
      OPEN 'ELIGIBILITY.EXCEPTIONS,CONV' TO ELIGIBILITY.EXCEPTIONS.CONV ELSE STOP       
OPEN 'CLIENT.SITES' TO CLIENT.SITES ELSE STOP 201,'CLIENT.SITES'
      OPEN 'ELIGIBILITY.EXCEPTIONS,CONV' TO ELIGIBILITY.EXCEPTIONS.CONV ELSE STOP 201
         OPEN 'CANCEL.REASONS' TO CANCEL.REASONS ELSE STOP 201,'CANCEL.REASONS'
OPEN "ME.CONTRACTS" TO YEAREND.CONTRACTS ELSE STOP
            OPEN "DEC.CONTRACTS" TO YEAREND.CONTRACTS ELSE STOP
      OPEN 'DEP' TO DEP.FILE ELSE EMSG<-1>=CO:'DEP'
      OPEN 'DICT','DEP' TO D.DEP ELSE EMSG<-1>=CO:'DICT, DEP'
      OPEN 'HDEP' TO HD.FILE ELSE EMSG<-1>=CO:"HDEP"
      OPEN 'UDEP' TO UD.FILE ELSE EMSG<-1>=CO:"UDEP"
      OPEN 'CTRL.FILE' TO CTRL.FILE ELSE EMSG<-1>=CO:'CTRL.FILE'
      OPEN 'CONTRACTS' TO CONTRACTS ELSE EMSG<-1>=CO:'CONTRACTS'
      OPEN 'MAINTENANCE' TO MAINTENANCE ELSE EMSG<-1>=CO:'MAINTENANCE'
      OPEN 'CON.TRANS' TO CT.FILE ELSE EMSG<-1>=CO:'CON.TRANS'
      OPEN 'MO.CON.TRANS' TO MCT.FILE ELSE EMSG<-1>=CO:'MO.CON.TRANS'
      OPEN 'MO.CON.TRANS,NON.CASH' TO MCT.NC ELSE EMSG<-1>=CO:'MO.CON.TRANS,NON.CASH'
      OPEN 'DICT CON.TRANS' TO D.CT.FILE ELSE EMSG<-1>=CO:'DICT CON.TRANS'
      OPEN 'M.TRANS' TO MT.FILE ELSE EMSG<-1>=CO:'M.TRANS'
      OPEN 'MO.M.TRANS' TO MMT.FILE ELSE EMSG<-1>=CO:'MO.M.TRANS'
      OPEN 'MO.M.TRANS,NON.CASH' TO MMT.NC ELSE EMSG<-1>=CO:'MO.M.TRANS,NON.CASH'
      OPEN 'DICT M.TRANS' TO D.MT.FILE ELSE EMSG<-1>=CO:'DICT M.TRANS'
      OPEN "DEP,BATCHES" TO BATCH.FILE ELSE EMSG<-1>=CO:'DEP,BATCH'
      OPEN 'CURR.BAL' TO CURR.BAL ELSE EMSG<-1>=CO:'CURR.BAL'
      OPEN 'CURR.BAL,MAINT' TO CURR.BAL.MAINT ELSE EMSG<-1>=CO:'CURR.BAL,MAINT'
      OPEN 'BANK.BAL' TO BANK.BAL ELSE EMSG<-1>=CO:'BANK.BAL'
      OPEN 'BANK.BAL.AUTO' TO BANK.BAL.AUTO ELSE EMSG<-1>=CO:'BANK.BAL.AUTO'
      OPEN 'LEND.CODES' TO LEND.CODES ELSE EMSG<-1>=CO:'LEND.CODES'
      OPEN 'DEV.CODES' TO DEV.CODES ELSE EMSG<-1>=CO:'DEV.CODES'
      OPEN 'PROJ.CODES' TO PROJ.CODES ELSE EMSG<-1>=CO:'PROJ.CODES'
      OPEN 'ADDR.MAST' TO ADDR.MAST ELSE EMSG<-1>=CO:'ADDR.MAST'
      OPEN 'COLLECT.LIST,IN' TO COLL.IN ELSE EMSG<-1>=CO:'COLLECT.LIST,IN'
      OPEN 'COLLECT.LIST' TO COLLECT.LIST ELSE EMSG<-1>=CO:'COLLECT.LIST'
      OPEN 'COLLECTORS' TO COLLECTORS ELSE EMSG<-1>=CO:'COLLECTORS'
      OPEN 'DICT','COLLECT.LIST' TO DICT.COLL ELSE EMSG<-1>=CO:'DICT COLLECT.LIST'
      OPEN 'R.HIST' TO R.HIST ELSE EMSG<-1>=CO:'R.HIST'
      OPEN 'R.HIST.LFC' TO R.HIST.LFC ELSE EMSG<-1>=CO:'R.HIST.LFC'
      OPEN 'R.HIST.SUN' TO R.HIST.SUN ELSE EMSG<-1>=CO:'R.HIST.SUN'
      OPEN 'OTHER.DATA' TO LITCHFIELD ELSE EMSG<-1>=CO:'OTHER.DATA'
      OPEN 'POOLS' TO POOLS ELSE EMSG<-1>=CO:'POOLS'
      OPEN 'LOW.BAL' TO LOW.BAL ELSE EMSG<-1>=CO:'LOW.BAL'
      OPEN 'LOW.BAL,MAINT' TO LOW.BAL.MAINT ELSE EMSG<-1>=CO:'LOW.BAL,MAINT'
      OPEN 'BANKRUPT' TO BANKRUPT ELSE EMSG<-1>=CO:'BANKRUPT'
      OPEN '&SAVEDLISTS&' TO SAVEDLISTS ELSE EMSG<-1>=CO:'&SAVEDLISTS&'
      OPEN 'STATIC' TO STATIC ELSE EMSG<-1>=CO:'STATIC'
      OPEN 'WEBAIS.XMIT' TO WEBXMIT ELSE EMSG<-1>=CO:'WEBAIS.XMIT'
      OPEN 'COLL.DATA' TO COLL.DATA ELSE EMSG<-1>=CO:'COLL.DATA'
      OPEN 'DELINQ.ME' TO DELINQ.ME ELSE EMSG<-1>=CO:'DELINQ.ME'
      OPEN 'SUREPAY' TO SUREPAY ELSE EMSG<-1>=CO:'SUREPAY'
      OPEN 'RAS.CUST.QUEUE' TO RAS.CUST.QUEUE ELSE EMSG<-1>=CO:'RAS.CUST.QUEUE'
      OPEN 'BANK.ACCT' TO BANK.ACCT.FILE ELSE EMSG<-1>=CO:'BANK.ACCT'
      OPEN 'CONTRACTS,PREVIEW' TO C.PREVIEW ELSE EMSG<-1>=CO:'CONTRACTS,PREVIEW'
      OPEN 'MAINTENANCE,PREVIEW' TO M.PREVIEW ELSE EMSG<-1>=CO:'MAINTENANCE,PREVIEW'
      OPEN 'CHARGES' TO CHARGES.FL ELSE EMSG<-1> = CO:'CHARGES'
      OPEN 'DICT','CHARGES' TO DICT.CHARGES ELSE EMSG<-1> = CO:'DICT CHARGES'
      OPEN 'CHARGES.INDEX' TO CHARGES.INDEX ELSE EMSG<-1> = CO:'CHARGES.INDEX'
      OPEN 'CHARGES.HIST' TO CHARGES.HIST.FL ELSE EMSG<-1> = CO:'CHARGES.HIST'
      OPEN 'CHARGES.HIST.INDEX' TO CHARGES.HIST.INDEX ELSE EMSG<-1> = CO:'CHARGES.HIST.INDEX'
      OPEN 'CHARGES,PREVIEW' TO CHARGES.PREVIEW ELSE EMSG<-1> = CO:'CHARGES,PREVIEW'
      OPEN 'CHARGE.TYPES' TO CHARGE.TYPES.FL ELSE EMSG<-1> = CO:'CHARGE.TYPES'
      OPEN 'BANK.RECON' TO BANK.RECON ELSE EMSG<-1> = CO:'BANK.RECON'
      OPEN 'MERCHACCT' TO MERCHACCT ELSE EMSG<-1> = CO:'MERCHACCT'
      OPEN 'ID.XREF' TO ID.XREF ELSE EMSG<-1> = CO:'ID.XREF'
      OPEN 'JODI.TEMP' TO TEMP.FL ELSE EMSG<-1> = CO:'JODI.TEMP'
      OPEN 'DRAWS,HIST' TO DRAW.FILE ELSE EMSG<-1>=CO:"DRAWS,HIST"
      OPEN 'LATE.CHARGE' TO LATE_CHARGE ELSE EMSG<-1>=CO:'LATE.CHARGE'
      OPEN 'COUPON' TO COUPON ELSE EMSG<-1>=CO:'COUPON'
      OPEN 'TRANS_TYPES' TO TRANS_TYPES ELSE EMSG<-1>=CO:'TRANS_TYPES'
      OPEN 'SBEACH.INT.RATE' TO SBEACH.INT.RATE ELSE EMSG<-1> = CO:'SBEACH.INT.RATE'
      OPEN 'CUSTOM.DATA' TO CUSTOM.DATA ELSE EMSG<-1> = CO:'CUSTOM.DATA'
      OPEN 'STATES' TO STATES ELSE EMSG<-1> = CO:'STATES'
      OPEN 'DFLT.PLACEMENTS' TO DFLT.PLACEMENTS ELSE EMSG<-1> = CO:'DFLT.PLACEMENTS'
      OPEN 'DFLT.AGREEMENT' TO DFLT.AGREEMENT ELSE EMSG<-1> = CO:'DFLT.AGREEMENT'
      OPEN 'LBX.EMAIL' TO LBX.EMAIL ELSE EMSG<-1> = CO:'LBX.EMAIL'
      OPEN 'NOTES.MAST' TO NOTES.MAST ELSE EMSG<-1> = CO:'NOTES.MAST'
      OPEN 'EFT.NSF.CTRL' TO EFT.NSF.CTRL ELSE EMSG<-1> = CO:'EFT.NSF.CTRL'
      OPEN 'CCF.XFER.HIST' TO CCF.XFER.HIST ELSE EMSG<-1> = CO:'CCF.XFER.HIST'
      OPEN 'CFX' TO CFX ELSE EMSG<-1> = CO:'CFX'
      OPEN 'CFX,PROCESSED' TO CFX.PROCESSED ELSE EMSG<-1> = CO:'CFX,PROCESSED'
      OPEN 'CHARGEBACKS' TO CHARGEBACKS ELSE EMSG<-1> = CO:'CHARGEBACKS'
      OPEN 'WEBUSER' TO WEBUSER ELSE EMSG<-1> = CO:'WEBUSER'
      OPEN 'CCF' TO CCF ELSE EMSG<-1> = CO:'CCF'
      OPEN 'COMPANY.TRANSID.CON.TRANS.INDEX' TO COMPANY.TRANSID.CON.TRANS.INDEX ELSE EMSG<-1> = CO:'COMPANY.TRANSID.CON.TRANS.INDEX'
      OPEN 'COMPANY.TRANSID.M.TRANS.INDEX' TO COMPANY.TRANSID.M.TRANS.INDEX ELSE EMSG<-1> = CO:'COMPANY.TRANSID.M.TRANS.INDEX'
